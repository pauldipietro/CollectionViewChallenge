<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CollectionViewChallenge.Views.CollectionViewChallengePage"
             BackgroundColor="Gray">
    <ContentPage.Content>
        <StackLayout Padding="0,100,0,30">
            <!-- Use your own layout and functionality here! -->
            <CollectionView ItemsSource="{Binding TimelineItems}"  Margin="10">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="5" BackgroundColor="White">
                            <!-- header -->
                            <StackLayout>
                                <Label Text="{Binding Member.FullName}" FontSize="Small" />
                                <Label Text="{Binding Created}" FontSize="Small" />
                            </StackLayout>
                            
                            <Label Text="{Binding Message}" FontSize="Small" />
                            <BoxView BackgroundColor="Green" />
                            
                            <StackLayout Orientation="Horizontal" Margin="5">
                                <Label Text="Like" FontSize="Small" />
                                <Label Text="Comment" FontSize="Small" HorizontalOptions="CenterAndExpand"/>
                                <Label Text="Share" FontSize="Small" />
                            </StackLayout>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>